# 0001：一个学习季固定一个 Pi 发布版本

- 状态：已接受
- 日期：2026-08-27

## 决定

第一学习季固定 Pi `v0.84.3`，通过 Git submodule 保存精确 commit。学习季内不追随 `main`。

## 原因

Book、证据地图和图必须引用同一实现，否则读者看到的源码与解释会漂移。

## 后果

升级版本必须开启新的迁移工作，重新验证证据和受影响章节；不能只修改版本字符串。
